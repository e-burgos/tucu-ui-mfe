import AuthCard from '../components/auth-card';
import LoginForm from '../features/login/forms/login-form';

const LoginPage = () => {
  return (
    <AuthCard>
      <LoginForm />
    </AuthCard>
  );
};

export default LoginPage;
